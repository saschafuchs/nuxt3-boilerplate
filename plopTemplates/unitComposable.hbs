import { describe, it, expect } from 'vitest'
import { use{{ pascalCase name }} } from './{{ camelCase name }}'

describe('use{{ pascalCase name }} composable', () => {
  it('should be defined', () => {
    expect(use{{ pascalCase name }}).toBeDefined()
  })
})
